# =============================================================================
# 환경변수 예시 파일
# =============================================================================
# 이 파일을 .env로 복사하여 실제 값을 설정하세요
# cp .env.example .env
# =============================================================================

# =============================================================================
# OpenAI API (AI 전략 생성용)
# =============================================================================
OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# Sentry 에러 추적 (선택사항)
# =============================================================================
# sentry.io에서 프로젝트 생성 후 DSN 발급
# 설정하지 않으면 Sentry 비활성화
SENTRY_DSN=

# =============================================================================
# Grafana 관리자 계정
# =============================================================================
# 프로덕션 환경에서 반드시 변경하세요!
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin123

# =============================================================================
# Redis 설정
# =============================================================================
# 개발 환경: 비워두면 인증 없이 연결
# 프로덕션: 반드시 강력한 비밀번호 설정!
REDIS_PASSWORD=

# Docker 내부에서는 기본값 사용 (주석 해제 불필요)
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_DB=0

# =============================================================================
# Cloudflare Tunnel (프로덕션 필수)
# =============================================================================
# Cloudflare Zero Trust 대시보드에서 터널 생성 후 토큰 발급
# https://one.dash.cloudflare.com/ → Networks → Tunnels → Create
CLOUDFLARE_TUNNEL_TOKEN=

# =============================================================================
# CORS 허용 도메인
# =============================================================================
# 쉼표로 구분하여 여러 도메인 설정 가능
# 프로덕션: 실제 도메인으로 변경 (예: https://your-domain.com)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost
